<div class="span11">

  <div class="head-title">
    <h2>
      ข้อมูลผู้ใช้
    </h2>
  </div>

  <div class="title-group">
    <div class="title">
      <h3>
        <div class="RG"></div>
        แก้ไขข้อมูลผู้ใช้
      </h3>
    </div>
  </div>


  <table class="table table-bordered">
    <thead>
    <tr>
      <th>ชื่อผู้ใช้</th>
      <th>อีเมล์</th>
      <th>วันที่ลงทะเบียน</th>
      <th>บทบาท</th>
      <th></th>
      <th></th>
    </tr>
    </thead>
    <tbody>
    <% @users.each do |user| %>
        <tr>
          <td><%= link_to user.name, user %></td>
          <td><%= user.email %></td>
          <td><%= user.created_at.to_date %></td>
          <td><%= user.roles.first.name.titleize unless user.roles.first.nil? %></td>
          <td>
            <a data-toggle="modal" href="#role-options-<%= user.id %>" class="btn btn-mini btn-inverse" type="button">เปลี่ยนบทบาท</a>
            <%= render user %>
          </td>
          <td><%= link_to("ยกเลิกผู้ใช้", user_path(user), :data => {:confirm => "Are you sure?"}, :method => :delete, :class => 'btn btn-mini btn-danger') unless user == current_user %></td>
        </tr>
    <% end %>
    </tbody>
  </table>
</div>


